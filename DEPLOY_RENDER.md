# ⚠️ 本文件已弃用（Deprecated）

原方案中需要把后端部署到 Render + Supabase，步骤较繁琐。

**现方案已简化为：前端直接连接你的 Supabase 免费数据库，无需任何后端服务器、无需部署。**

你只需要：
1. 打开工作台链接（CloudStudio 静态部署）；
2. 输入一个自己定的「云端空间密钥」（如 `my-class-2026`）即进入；
3. 数据自动存到你自己的 Supabase 免费库。

若登录提示 `401 / 云端读取失败`，说明内置的 anon key 已失效，进入「基础设置 → 云端同步」点击 **anon public** 行的 **Copy** 按钮复制最新密钥粘贴保存即可（详见对话指引）。
